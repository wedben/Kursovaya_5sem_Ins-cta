# 🚀 Быстрая настройка базы данных

## Вариант 1: Установка через Homebrew (рекомендуется для macOS)

```bash
# Установка PostgreSQL
brew install postgresql@14

# Запуск службы PostgreSQL
brew services start postgresql@14

# Или запуск вручную
pg_ctl -D /opt/homebrew/var/postgresql@14 start
```

## Вариант 2: Установка через официальный установщик

1. Скачайте PostgreSQL с https://www.postgresql.org/download/macosx/
2. Установите через установщик
3. Запомните пароль, который вы установите для пользователя postgres

## После установки PostgreSQL

### 1. Настройте файл .env

Откройте файл `.env` и укажите пароль (если вы его устанавливали):

```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=insects_db
DB_USER=postgres
DB_PASSWORD=ваш_пароль
```

Если пароля нет, оставьте `DB_PASSWORD=` пустым.

### 2. Запустите автоматическую настройку

```bash
python3 setup_db.py
```

Этот скрипт автоматически:
- ✅ Создаст базу данных
- ✅ Создаст все таблицы
- ✅ Добавит тестовые данные

### 3. Готово! Запустите приложение

```bash
python3 app.py
```

Откройте в браузере: http://localhost:5001

---

## Если у вас уже есть PostgreSQL

Просто выполните:

```bash
# 1. Настройте .env файл (укажите правильные параметры)
# 2. Запустите настройку
python3 setup_db.py
```

---

## Если у вас уже есть база данных с данными

1. Откройте `.env` и укажите параметры вашей базы данных
2. Убедитесь, что структура таблиц соответствует `create_tables.sql`
3. Запустите приложение - оно будет работать с вашими данными

