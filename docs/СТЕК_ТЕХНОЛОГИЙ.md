# Стек технологий проекта

## 📋 Обзор

Проект использует современный стек технологий для создания полнофункционального веб-приложения с базой данных. Архитектура построена по принципу разделения на три уровня: Frontend, Backend и Database.

---

## 🎨 FRONTEND (Клиентская часть)

### Vue.js 3
**Версия:** 3.x (через CDN)  
**URL:** `https://unpkg.com/vue@3/dist/vue.global.js`

**Что это:**
- Прогрессивный JavaScript-фреймворк для создания пользовательских интерфейсов
- Реактивная система данных - автоматическое обновление UI при изменении данных
- Компонентный подход к разработке

**Зачем используется:**
- ✅ Создание интерактивного пользовательского интерфейса
- ✅ Реактивное связывание данных (two-way data binding)
- ✅ Динамическое отображение фильтров и результатов поиска
- ✅ Обработка пользовательских действий без перезагрузки страницы

**Примеры использования в проекте:**
```javascript
// Реактивные данные
data() {
    return {
        selectedType: null,
        results: [],
        searchParams: { ... }
    }
}

// Директивы Vue
<div v-if="selectedType === 'dragonfly'">...</div>
<button @click="searchInsects()">Найти</button>
<span v-for="insect in results">{{ insect.name_ru }}</span>
```

**Преимущества:**
- Легковесный (можно использовать через CDN)
- Простой синтаксис
- Отличная производительность
- Хорошая документация

---

### HTML5
**Стандарт:** HTML5

**Использование:**
- Семантическая разметка страницы
- Формы для ввода данных
- Мета-теги для адаптивности (`viewport`)
- Поддержка современных веб-стандартов

**Особенности в проекте:**
- Использование Jinja2 шаблонов Flask
- Семантические теги: `<header>`, `<main>`, `<section>`
- Формы с валидацией
- Поддержка кириллицы (UTF-8)

---

### CSS3
**Стандарт:** CSS3

**Использование:**
- Стилизация интерфейса
- Адаптивный дизайн (responsive design)
- Анимации и переходы
- Flexbox и Grid для компоновки

**Особенности в проекте:**
- Кастомные CSS переменные
- Медиа-запросы для мобильных устройств
- Стили для карточек результатов
- Интерактивные элементы (hover, active состояния)

**Примеры:**
```css
/* Адаптивность */
@media (max-width: 768px) {
    .filters-grid {
        grid-template-columns: 1fr;
    }
}

/* Анимации */
.insect-btn:hover {
    transform: scale(1.05);
    transition: transform 0.2s;
}
```

---

### JavaScript (ES6+)
**Версия:** Современный JavaScript (ES6+)

**Использование:**
- Асинхронные запросы к API (Fetch API)
- Обработка событий
- Манипуляции с DOM (через Vue.js)
- Работа с JSON данными

**Примеры в проекте:**
```javascript
// Fetch API для запросов
async searchInsects() {
    const response = await fetch('/api/search', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ type: this.selectedType, params: this.searchParams })
    });
    const data = await response.json();
    this.results = data.results;
}
```

---

## ⚙️ BACKEND (Серверная часть)

### Python 3
**Версия:** Python 3.x

**Почему Python:**
- ✅ Простой и читаемый синтаксис
- ✅ Богатая экосистема библиотек
- ✅ Отличная поддержка работы с базами данных
- ✅ Активное сообщество разработчиков

**Использование в проекте:**
- Основной язык программирования для backend
- Обработка бизнес-логики
- Работа с базой данных
- Обработка HTTP запросов

---

### Flask 3.0.0
**Версия:** 3.0.0  
**Тип:** Микро-веб-фреймворк

**Что это:**
- Легковесный веб-фреймворк для Python
- Минималистичный подход - только необходимый функционал
- Расширяемый через расширения (extensions)

**Зачем используется:**
- ✅ Создание RESTful API endpoints
- ✅ Обработка HTTP запросов (GET, POST)
- ✅ Рендеринг HTML шаблонов
- ✅ Возврат JSON ответов

**API Endpoints в проекте:**
```python
@app.route('/')                    # Главная страница
@app.route('/api/search', methods=['POST'])           # Поиск насекомых
@app.route('/api/all/<insect_type>', methods=['GET']) # Все насекомые
@app.route('/api/filter-options/<insect_type>')       # Опции фильтров
```

**Преимущества:**
- Легковесный (минимальные зависимости)
- Гибкий (можно добавить только нужное)
- Простой в изучении
- Хорошо подходит для REST API

**Пример использования:**
```python
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/api/search', methods=['POST'])
def search_insects():
    data = request.json
    results = db.search_insects(data['type'], data['params'])
    return jsonify({'results': results})
```

---

### Werkzeug 3.0.1
**Версия:** 3.0.1  
**Тип:** WSGI утилита

**Что это:**
- WSGI (Web Server Gateway Interface) библиотека
- Основа Flask (Flask построен на Werkzeug)
- Обрабатывает HTTP запросы и ответы

**Использование:**
- Автоматически используется Flask
- Обработка HTTP протокола
- Управление сессиями
- Валидация данных

---

### python-dotenv 1.0.0
**Версия:** 1.0.0

**Что это:**
- Библиотека для работы с переменными окружения из `.env` файлов

**Зачем используется:**
- ✅ Безопасное хранение конфигурации (пароли, ключи)
- ✅ Удобное управление настройками для разных окружений
- ✅ Не коммитить секретные данные в Git

**Использование в проекте:**
```python
from dotenv import load_dotenv
import os

load_dotenv()  # Загружает .env файл

DB_CONFIG = {
    'host': os.getenv('DB_HOST', 'localhost'),
    'database': os.getenv('DB_NAME', 'insects_db'),
    # ...
}
```

**Файл `.env`:**
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=insects_db
DB_USER=wedben
DB_PASSWORD=
```

---

## 🗄️ БАЗА ДАННЫХ

### PostgreSQL 14
**Версия:** 14.20 (Homebrew)  
**Тип:** Объектно-реляционная СУБД (ORDBMS)

**Что это:**
- Мощная, открытая система управления базами данных
- Поддержка SQL стандарта
- Расширенные возможности: триггеры, процедуры, представления

**Почему PostgreSQL:**
- ✅ Надежность и стабильность
- ✅ Богатый функционал (триггеры, хранимые процедуры, представления)
- ✅ Отличная производительность
- ✅ Поддержка сложных типов данных
- ✅ Полнотекстовый поиск
- ✅ Расширяемость (можно писать функции на разных языках)

**Использование в проекте:**
- Хранение данных о насекомых (стрекозы, жуки, бабочки)
- Хранение данных о наблюдениях
- Связи между сущностями (foreign keys)
- Индексы для оптимизации запросов

**Структура БД:**
```
insects_db/
├── dragonflies (54 записи)
├── beetles (35 записей)
├── butterflies (25 записей)
├── Пользователь
├── МестоНаблюдения
├── ВидНасекомого
├── Биотоп (7 записей)
├── СтатусТочности (4 записи)
├── Наблюдение
├── Идентификация
└── ВидНасекомого_Биотоп
```

**Особенности:**
- Нормализация до 3NF
- Индексы на часто используемых полях
- Ограничения целостности (constraints)
- Триггеры для автоматических действий
- Хранимые процедуры для сложных операций
- Представления (views) для упрощения запросов

---

### psycopg2-binary 2.9.9
**Версия:** 2.9.9  
**Тип:** Адаптер PostgreSQL для Python

**Что это:**
- Официальный драйвер PostgreSQL для Python
- Реализует DB-API 2.0 стандарт
- `-binary` версия включает скомпилированные библиотеки

**Зачем используется:**
- ✅ Подключение к PostgreSQL из Python
- ✅ Выполнение SQL запросов
- ✅ Получение результатов запросов
- ✅ Управление транзакциями

**Использование в проекте:**
```python
import psycopg2
from psycopg2.extras import RealDictCursor

# Подключение
conn = psycopg2.connect(
    host='localhost',
    database='insects_db',
    user='wedben',
    password=''
)

# Выполнение запроса
cursor = conn.cursor(cursor_factory=RealDictCursor)
cursor.execute("SELECT * FROM dragonflies WHERE color ILIKE %s", ['%синий%'])
results = cursor.fetchall()  # Возвращает список словарей
```

**Особенности:**
- **RealDictCursor** - возвращает результаты в виде словарей (удобно для JSON)
- Параметризованные запросы (защита от SQL-инъекций)
- Поддержка транзакций
- Автоматическое управление подключениями

**Пример безопасного запроса:**
```python
# ✅ ПРАВИЛЬНО - параметризованный запрос
cursor.execute("SELECT * FROM dragonflies WHERE size_min >= %s", [30])

# ❌ НЕПРАВИЛЬНО - уязвимо к SQL-инъекциям
cursor.execute(f"SELECT * FROM dragonflies WHERE size_min >= {30}")
```

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ИНСТРУМЕНТЫ

### Jinja2
**Тип:** Шаблонизатор (встроен в Flask)

**Использование:**
- Рендеринг HTML шаблонов
- Передача данных из Python в HTML
- Условная логика в шаблонах

**Пример:**
```html
<!-- templates/index.html -->
{% raw %}
<div v-if="selectedType === 'dragonfly'">
    <!-- Vue.js код -->
</div>
{% endraw %}
```

**Особенность:** Использование `{% raw %}` для предотвращения конфликта с Vue.js синтаксисом `{{ }}`

---

### Регулярные выражения (re)
**Модуль:** `re` (встроен в Python)

**Использование:**
- Извлечение структурированных данных из текстовых описаний
- Парсинг информации о насекомых из поля `description`

**Пример в проекте:**
```python
import re

# Извлечение цвета глаз из описания
matches = re.findall(r'Цвет глаз:\s*([^;]+)', description)
```

---

### Pandas (для импорта данных)
**Использование:** В скриптах импорта данных из Excel

**Зачем:**
- Чтение Excel файлов
- Обработка и очистка данных
- Преобразование данных для вставки в БД

---

## 📊 АРХИТЕКТУРА ПРОЕКТА

### Трехзвенная архитектура (Three-Tier Architecture)

```
┌─────────────────────────────────────────┐
│  TIER 1: PRESENTATION LAYER             │
│  (Уровень представления)                │
│                                         │
│  Vue.js 3 + HTML5 + CSS3               │
│  - Пользовательский интерфейс          │
│  - Обработка пользовательских действий │
│  - Отображение результатов              │
└──────────────┬──────────────────────────┘
               │ HTTP/JSON
               ↓
┌─────────────────────────────────────────┐
│  TIER 2: BUSINESS LOGIC LAYER          │
│  (Уровень бизнес-логики)                │
│                                         │
│  Flask 3.0.0 + Python 3                 │
│  - RESTful API                          │
│  - Обработка запросов                  │
│  - Валидация данных                     │
│  - Формирование ответов                │
└──────────────┬──────────────────────────┘
               │ SQL запросы
               ↓
┌─────────────────────────────────────────┐
│  TIER 3: DATA LAYER                    │
│  (Уровень данных)                       │
│                                         │
│  PostgreSQL 14 + psycopg2              │
│  - Хранение данных                     │
│  - Выполнение запросов                  │
│  - Обеспечение целостности данных      │
└─────────────────────────────────────────┘
```

---

## 🔄 ПОТОК ДАННЫХ

### Пример: Поиск насекомых

```
1. ПОЛЬЗОВАТЕЛЬ
   ↓
   Выбирает тип насекомого и параметры поиска
   ↓
2. VUE.JS (Frontend)
   ↓
   Собирает данные из формы
   ↓
   Отправляет POST запрос: /api/search
   ↓
3. FLASK (Backend)
   ↓
   Получает JSON данные
   ↓
   Вызывает db.search_insects()
   ↓
4. DATABASE.PY
   ↓
   Формирует SQL запрос
   ↓
   Создает подключение через psycopg2
   ↓
5. POSTGRESQL
   ↓
   Выполняет SQL запрос
   ↓
   Возвращает результаты
   ↓
6. DATABASE.PY
   ↓
   Преобразует результаты в список словарей
   ↓
7. FLASK
   ↓
   Формирует JSON ответ
   ↓
8. VUE.JS
   ↓
   Обновляет интерфейс с результатами
   ↓
9. ПОЛЬЗОВАТЕЛЬ
   ↓
   Видит найденных насекомых
```

---

## 📦 ЗАВИСИМОСТИ ПРОЕКТА

### Python зависимости (requirements.txt)
```
Flask==3.0.0              # Веб-фреймворк
Werkzeug==3.0.1           # WSGI утилита (зависимость Flask)
psycopg2-binary==2.9.9    # Адаптер PostgreSQL
python-dotenv==1.0.0       # Работа с .env файлами
```

### Frontend зависимости
- **Vue.js 3** - через CDN (не требует установки)
- **HTML5/CSS3** - встроены в браузер
- **JavaScript ES6+** - встроен в браузер

### Установка зависимостей
```bash
# Python зависимости
pip install -r requirements.txt

# PostgreSQL (через Homebrew на Mac)
brew install postgresql@14
```

---

## 🎯 ПРЕИМУЩЕСТВА ВЫБРАННОГО СТЕКА

### Frontend (Vue.js)
- ✅ Легковесный (можно использовать через CDN)
- ✅ Простой в изучении
- ✅ Реактивность из коробки
- ✅ Хорошая производительность
- ✅ Не требует сборки (можно использовать без Webpack/Vite)

### Backend (Flask + Python)
- ✅ Простота и читаемость кода
- ✅ Быстрая разработка
- ✅ Легковесный фреймворк
- ✅ Гибкость (можно добавить только нужное)
- ✅ Отличная поддержка REST API

### Database (PostgreSQL)
- ✅ Надежность и стабильность
- ✅ Богатый функционал
- ✅ Отличная производительность
- ✅ Поддержка сложных запросов
- ✅ Бесплатный и open-source

---

## 🔐 БЕЗОПАСНОСТЬ

### Реализованные меры безопасности:

1. **Параметризованные SQL запросы**
   - Защита от SQL-инъекций
   - Использование `%s` вместо конкатенации строк

2. **Переменные окружения**
   - Секретные данные в `.env` файле
   - `.env` в `.gitignore` (не коммитится в Git)

3. **Валидация данных**
   - Проверка типов насекомых
   - Валидация входных параметров

4. **Локальная база данных**
   - Доступ только с локального компьютера
   - Не доступна из интернета

---

## 📈 ПРОИЗВОДИТЕЛЬНОСТЬ

### Оптимизации:

1. **Индексы в PostgreSQL**
   - На часто используемых полях (color, habitat, season)
   - Ускоряют поиск

2. **RealDictCursor**
   - Эффективное преобразование результатов
   - Меньше обработки на стороне Python

3. **Параметризованные запросы**
   - Кэширование планов выполнения
   - Повышение скорости повторных запросов

---

## 🚀 РАЗВЕРТЫВАНИЕ

### Локальная разработка:
```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Настройка БД
# Создание базы данных и таблиц через SQL скрипты

# 3. Запуск приложения
python app.py
# Приложение доступно на http://localhost:5001
```

### Production (потенциально):
- Flask приложение: Gunicorn или uWSGI
- PostgreSQL: на отдельном сервере или облачном сервисе
- Frontend: можно собрать статические файлы или использовать CDN
- Reverse proxy: Nginx для статических файлов и проксирования

---

## 📚 РЕЗЮМЕ

**Frontend стек:**
- Vue.js 3 (CDN)
- HTML5
- CSS3
- JavaScript ES6+

**Backend стек:**
- Python 3
- Flask 3.0.0
- Werkzeug 3.0.1
- python-dotenv 1.0.0

**Database стек:**
- PostgreSQL 14.20
- psycopg2-binary 2.9.9

**Архитектура:**
- Трехзвенная архитектура
- RESTful API
- Разделение ответственности между слоями

**Преимущества:**
- Современные технологии
- Простота разработки и поддержки
- Хорошая производительность
- Безопасность
- Масштабируемость

